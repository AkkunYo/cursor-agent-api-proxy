name: Build and Publish Docker Image

on:
  push:
    branches:
      - main
      - master
    tags:
      - 'v*'
  pull_request:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build-and-push-image:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: 获取版本号并写入 VERSION
        run: |
          # 获取仓库最新 tag
          TAG=$(git tag --sort=-v:refname | head -n 1)

          if [ -z "$TAG" ]; then
            # 兜底方案: 基于提交数计算版本
            BUILD_NUM=$(git rev-list --count HEAD)
            MAJOR=$((BUILD_NUM / 100 + 1))
            MINOR=$(((BUILD_NUM % 100) / 10))
            PATCH=$((BUILD_NUM % 10))
            TAG="v$MAJOR.$MINOR.$PATCH"
          fi

          # 写入环境变量
          echo "VERSION=$TAG" >> $GITHUB_ENV
          echo "$TAG" > VERSION
          echo "最终版本: $TAG"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 登录阿里云镜像仓库
        uses: docker/login-action@v3
        with:
          registry: registry.cn-hangzhou.aliyuncs.com
          username: ${{ secrets.ALIYUN_REGISTRY_USERNAME }}
          password: ${{ secrets.ALIYUN_REGISTRY_PASSWORD }}

      - name: 构建并推送镜像
        uses: docker/build-push-action@v5
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name != 'pull_request' }}
          tags: |
            registry.cn-hangzhou.aliyuncs.com/zkyml/cursor-agent-api-proxy:latest
            registry.cn-hangzhou.aliyuncs.com/zkyml/cursor-agent-api-proxy:${{ env.VERSION }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: false
          sbom: false
